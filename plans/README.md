# Implementation Plans

Generated by the improve skill on 2026-08-13 at commit `89708a7`. Execute
in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your
row when done (Status values: TODO | IN PROGRESS | DONE | BLOCKED
(with one-line reason) | REJECTED (with one-line rationale)).

Every plan was written against commit `89708a7` and carries its own drift
check — run `git diff --stat 89708a7..HEAD -- <plan's in-scope paths>`
first and STOP on mismatch.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Client submit error-banner + Web3Forms/safeText coverage | P1 | S | — | TODO |
| 002 | Same-origin gate on /api/events | P1 | S | — | TODO |
| 003 | Actionable server 400 fields + client attribution truncation | P1 | M | 001 | TODO |
| 004 | Turnstile idempotency key per token (verify + fix) | P1 | S | — | TODO |
| 005 | Bound the Web3Forms wait before thank-you redirect | P1 | S | 001 | TODO |
| 006 | Refresh ARCHITECTURE/PLAN/PRODUCT/QUALITY to post-Slice-2 reality | P2 | S | — | TODO |
| 007 | Theme toggle + mobile menu + analytics coverage | P2 | M | — | TODO |
| 008 | Test the env-wiring boundary (buildAuditDeps) | P2 | M | 004 | TODO |
| 009 | Cache npm deps in CI | P2 | S | — | TODO |
| 010 | Commit registry decision + lockfile-consistency guard | P2 | S | — | TODO |
| 011 | Analytics Engine query reference (funnel) | P2 | S | — | TODO |
| 012 | Case-study intake guide (template + honesty checklist) | P2 | S | — | TODO |
| 013 | UTM channel dimension on audit_started | P2 | S | 002 | TODO |
| 014 | Read-only leads funnel view (founder ops) | P3 | S | — | TODO |
| 015 | npm advisory monitoring (dependabot + CI audit) | P3 | S | 010 (recommended) | TODO |
| 016 | Cap the rate-limiter key map | P3 | S | — | TODO |
| 017 | Client↔server drift guard for payload keys + event names | P3 | S | 013 | TODO |
| 018 | Remove dead submittedAt param / now dep | P3 | S | — | TODO |
| 019 | Remove unreferenced @cloudflare/workers-types | P3 | S | — | TODO |

## Dependency notes

- **001 → 003, 005**: characterization/coverage of the client submit path
  must land before production changes to that path (003 per-field 400
  rendering; 005 redirect race) — red-protection first.
- **004 → 008**: plan 008 moves the `onRequest` wiring (which plan 004
  changes) into `buildAuditDeps`; land 004 first so the moved code is the
  final shape. 004 and 008 both touch `functions/api/audit.ts` — never run
  them concurrently.
- **002 → 013**: both touch the events path; the origin gate lands first so
  the UTM payload change is already protected. 013's doc follow-up lives in
  plan 011's maintenance note.
- **013 → 017**: the drift guard's client event-payload literal must match
  the final key set including plan 013's utm keys.
- **010 → 015**: the CI audit step relies on the npmjs `--registry` flag
  and the registry documentation that plan 010 establishes (works without
  it, but land 010 first for coherent review).
- **018 vs 008**: both touch `AuditDeps` — non-conflicting (008 never adds
  `now`); can run in either order but not concurrently in the same file
  region without a rebase.
- Plans 003 and 004 both add client constants mirroring server limits —
  no conflict (different constants), but reviewers should confirm the
  mirror-comment convention is consistent.

## Verification lanes (all plans)

- Targeted: `node --test tests/<file>.mjs` per plan.
- Affected route: `node scripts/verify-affected.mjs --file <path>` — routes
  `src/**` + `tests/audit-retry.test.mjs` to the app lane, `functions/**` +
  `supabase/**` + `tests/audit-function.test.mjs` to the functions lane,
  `docs/**` to the workflow/project-contract lanes.
- Full gate (required for every plan at completion): `bash scripts/verify.sh`.
- No lint/formatter exists in this repo — match the file's existing style
  (2-space, single quotes, semicolons).
- Do not push or open PRs unless the operator instructed it; commit per
  plan with conventional messages (e.g. `fix(audit): ...`, `docs(...)`,
  `test(...)`, `ci: ...`).

## Findings considered and rejected (from the audit that produced these plans)

- **Client-side validation gaps vs server**: client validation is UX-only by
  design (server authoritative) — not a finding; plan 003 only surfaces
  server rejections and prevents the avoidable 400 class.
- **Events endpoint 501-degrade**: deliberate degrade-by-design (runbook
  failure semantics) — not a finding.
- **Heavy verification scripting / harness tests**: deliberate project
  choice (CONTRIBUTING "Workflow-policy changes") — judged for correctness,
  not taste.
- **Honeypot + per-IP rate limiting as the only abuse gates on /api/audit**:
  documented decision (Turnstile is the primary gate; per-isolate limiter
  is a documented limitation) — not a finding; plan 002 closes the
  analogous gap on /api/events where no primary gate exists.
- **`verify.sh`'s unreachable `npm run ci` fallback**: harmless template
  residue (project-verify.sh is exec'd before it) — not worth a plan.
- **npmmirror lockfile as a bug**: likely deliberate (founder network) —
  reframed as plan 010 (document + guard) instead of a lockfile churn.
- **Zaraz absence**: accepted deferred decision (ARCHITECTURE.md, launch
  plan §5.8) — plan 006 only fixes PRODUCT.md's stale contradiction.
- **Font/visual decisions**: settled in DESIGN.md — plan 006 re-dates the
  PRODUCT.md open-decisions list accordingly.
- **Adding UTM to every event**: rejected in plan 013's scope (data
  minimization; only audit_started carries the channel).
- **Fire-and-forget navigation before notifyWeb3Forms**: strictly better
  UX but deviates from the written plan §5.6 — recorded as a future option
  in plan 005's maintenance notes, not implemented.

## Notes for the operator

- Plans 006, 011, 012 are doc-only (S) and can be executed by any agent
  with zero code risk.
- Plans 004 (investigate-first) and 010 have negative-path evidence steps —
  the executor records observations in the commit/report.
- CI-side evidence (009, 015) completes only after an operator push; the
  plan rows should record that separately from local gates.
- After all plans: re-run `bash scripts/verify.sh` from a clean checkout
  (or `npm ci` + the full gate) as the final integration proof.
