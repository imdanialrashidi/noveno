# Implementation Plans — Noveno improve cycle 3

Generated by the improve skill on 2026-08-21 against commit `2df2e69bb6`. This is the third audit
cycle for this repository (cycles 1–2 produced plans 001–031 across two rounds; all were executed
and their files removed after completion — see git history, commit `2df2e69bb6` deleted the old
`plans/`).

Execute in the order below unless dependencies say otherwise. Each executor: read your plan fully
before starting, honor its STOP conditions, run every verification command, and update your row
when done.

## Execution order & status

| Plan | Title                                                                                                                                | Priority | Effort | Depends on                               | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------ | ---------------------------------------- | ------ |
| 001  | Align D‑01 server-email handshake (`status:"sent"` completes journey; single-source labels; receipt + explicit `validated_at`)       | P1       | M      | —                                        | DONE   |
| 002  | Audit-form silent-rejection traps (`maxChannels`=enum size, visible channel errors, attribution clamps)                              | P1       | S–M    | none (re-read excerpts if after 001)     | DONE   |
| 003  | UI correctness bundle (blog prev/next unswap, Turnstile container visibility, `jalaliYear` Asia/Tehran)                              | P1       | S      | none                                     | DONE   |
| 004  | Server hardening cluster (honeypot value shapes, `no-store`+`nosniff` API headers, bounded limiter map)                              | P2       | S–M    | none                                     | DONE   |
| 005  | Test integrity (tautology fix, lane-coverage guard + routing, Turnstile lifecycle tests, token TTL)                                  | P2       | M      | none (coordinates with 001)              | DONE   |
| 006  | Build pipeline efficiency (CI npm cache, official-registry `.npmrc` + relock, images: sources out of `public/`)                      | P2       | M      | — (must precede 008)                     | DONE   |
| 007  | Repo hygiene (purge tracked `.noveno-backups`, `.editorconfig`, single canonical-origin constant)                                    | P3       | S      | none                                     | DONE   |
| 008  | Formatting baseline (Prettier + astro plugin, normalize commit, CI enforcement)                                                      | P3       | M      | 006 (clean registry), 007 (editorconfig) | DONE   |
| 009  | Font-preload measurement spike on image-LCP work routes (adopt-or-revert by lab numbers)                                             | P3       | S–M    | after 006/008 for a clean tree           | DONE — measured baseline vs low (mobile work_noveno-website LCP 2186→2190, desktop 516→513, no ≥3% win; reverted, homepage stays high) |
| 010  | Docs reconcile (README structural summary + directory map, spec `/insights` annotations, PRODUCT open decisions, CHANGELOG backfill) | P2       | S–M    | after code plans 001–008                 | DONE   |
| 011  | Work-collection draft gate (parity with blog; schema + pages + sitemap + tests)                                                      | P3       | S–M    | none                                     | DONE   |
| 012  | OG toolchain preflight (`requirements-og.txt`, `check-og-env.sh`, chained into `generate:og`)                                        | P3       | S      | none                                     | DONE   |
| 013  | Machine-checked doc truth (4 invariants in the project-contract gate)                                                                | P3       | S–M    | 010 (hard)                               | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 before founder provisioning**: `docs/ops/setup-checklist.md` §2a instructs setting
  `RESEND_API_KEY`; until 001 lands that flip breaks the conversion journey. Nothing else blocks it.
- **006 before 008**: formatting adds devDependencies; the registry should be pinned and audited
  first so the lockfile stays clean.
- **010 after the code settles** (001–008): corrected docs must describe final state.
- **013 immediately after 010**: its invariants assert exactly the facts 010 fixes; running it
  earlier fails by design.
- 002/003 touch neighboring audit-form code but are independent; if executed concurrently,
  001's Step 3 area of `src/scripts/audit/index.ts` overlaps 003's Step 3 — prefer sequential.
- Recommended linear sequence: 001 → 002 → 003 → 005 → 004 → 006 → 007 → 011 → 008 → 012 → 009 → 010 → 013.

## Findings considered and rejected (do not re-audit)

- **Contact-page form duplication**: no form exists on /contact (Spec §41 removed it); the real duplication is the ChannelLink cluster — judged cosmetic, not worth a plan this cycle.
- **Dead exports in `src/data/*`**: every export verified imported somewhere.
- **Missing RSS feed**: shipped at `src/pages/rss.xml.ts`; drafts excluded.
- **MultiSelect missing error element**: mis-attribution during audit — the element exists; the missing piece is error COPY, fixed in plan 002.
- **Per-isolate rate limiting / events origin-guard strength**: documented accepted tradeoffs (Cloudflare isolate semantics); only the unbounded key map was actionable (plan 004).
- **Hardcoded founder inbox/from defaults in D‑01 email path**: contact facts are public by design; settings-order footgun noted inside plan 001's checklist edit.
- **Receipt HMAC key separation + automated TTL verification**: deferred deliberately until the D‑01 cutover decides receipts' fate (rationale in plan 001's Maintenance notes).
- **Biome as formatter**: no stable `.astro` support at decision time (plan 008 uses Prettier).
- **Porting OG rendering from Python/Pillow to Node**: rejected — Persian RTL shaping fidelity risk on brand social cards (plan 012 rationale).
- **Server-side truncation of over-limit attribution**: rejected — mutating data crossing a trust boundary is worse than client-side clamping (plan 002).

## Verification baseline

Every plan's commands assume: `npm run check` → build → tests ordering (pinned by
`tests/gate-order.test.mjs`; structural suites need fresh `dist/`). Canonical gate:
`bash scripts/verify.sh`. CI mirror: `.github/workflows/quality.yml`.
